# openapi-typescript@7 的 peer 仍限定 typescript@^5.x，而本项目用 typescript@6，
# 直接 npm install / npm ci 会 ERESOLVE 失败（仅 gen:api 脚本用到它，运行时无影响）。
# 在此统一放开 peer 解析，免得每个调用点（Makefile / CI / 本地）都要手写 --legacy-peer-deps。
legacy-peer-deps=true
